<% unless user.current_user? current_user %>
  <div id="follow_form">
  <% if current_user.following? user %>
    <%= render "unfollow", user: user,
      relation_do: relation_do %>
  <% else %>
    <%= render "follow", user: user,
      relation_do: relation_do %>
  <% end %>
  </div>
<% end %>
